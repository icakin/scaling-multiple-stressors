# ── Prompts (internal working notes, not for distribution) ──
prompts/

# ── Internal, not for distribution ──
reviews/
ele_submission/
.claude/

# ── macOS ──
.DS_Store

# ── R / RStudio ──
.Rhistory
.RData
.RDataTmp
.Rproj.user/
*.Rproj.user

# ── Quarto / LaTeX build artifacts ──
manuscript/_output/
manuscript/.quarto/
manuscript/**/*.quarto_ipynb
manuscript/*_files/
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz

# ── Compiled Stan model caches (build artifacts, not results) ──
results/stan/*.rds

# ── Temp files ──
*.tmp
.mailmap

# ── Raw sequencing delivery (7.7 GB; archived at 10.5281/zenodo.8289513) ──
submission/ena/raw/

# ── Claude desktop app scratch ──
Claude outputs/
